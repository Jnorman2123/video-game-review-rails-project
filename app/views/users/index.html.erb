<h1>Users Index</h1> 

<% @users.each do |user| %> 
    <%= link_to user.username, user_path(user)%>
    <%= reviews_posted(user) %>
    <% if user.admin %>
        <p>*Admin*</p>
    <% end %>
    <br>
<% end %>